services:
  - type: web
    name: 2fa-authentication
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    pythonVersion: 3.11
    envVars:
      - key: SECRET_KEY
        generateValue: true
